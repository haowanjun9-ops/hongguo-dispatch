# 星广报表数据自动提取 - 使用说明

## 📋 任务概述

自动提取星广报表（https://apex.whaleidea.cn/star/report）的52页数据，并筛选出消耗>2000的记录。

## 📁 文件清单

| 文件名 | 说明 |
|--------|------|
| `auto_extract_browser_console.js` | 浏览器控制台执行的主要提取脚本 |
| `process_extracted_data.py` | 数据后处理Python脚本 |
| `extract_star_report.py` | 辅助说明脚本 |

## 🚀 快速开始

### 方法一：浏览器控制台执行（推荐）

**步骤1：打开报表页面**
- 在浏览器中访问：https://apex.whaleidea.cn/star/report
- 确保已登录，数据已加载完成

**步骤2：打开开发者工具**
- 按 `F12` 或右键选择"检查"
- 切换到 `Console`（控制台）标签

**步骤3：执行提取脚本**
```bash
# 在控制台中执行以下命令加载脚本：
# 方法A：直接粘贴脚本内容
# 打开 /workspace/auto_extract_browser_console.js，全选复制内容，粘贴到控制台执行

# 方法B：使用fetch加载（如果支持）
fetch('/workspace/auto_extract_browser_console.js')
  .then(r => r.text())
  .then(eval)
```

**步骤4：等待提取完成**
- 脚本会自动循环提取所有52页数据
- 控制台会显示进度：
  ```
  ✓ 第 1/52 页: 提取 20 条记录，累计 20 条
  ✓ 第 2/52 页: 提取 18 条记录，累计 38 条
  ...
  ```
- 预计耗时：2-3分钟

**步骤5：保存数据**
```javascript
// 在控制台执行以下命令复制数据
copy(window.ALL_DATA_JSON)

// 然后手动粘贴到文件
// /workspace/all_data.json
```

**步骤6：运行数据后处理**
```bash
cd /workspace
python3 process_extracted_data.py
```

### 方法二：Python自动化（需要MCP浏览器接口）

如果环境支持 `run_mcp` 工具：

```python
# Python脚本可以调用 browser_evaluate 和 browser_wait_for
# 参考 extract_star_report.py 中的逻辑
```

## 📊 输出文件

执行完成后会生成以下文件：

| 文件名 | 说明 |
|--------|------|
| `all_data.json` | 所有提取的原始数据（JSON格式） |
| `all_data_clean.json` | 清理后的完整数据 |
| `high_cost_records.csv` | 筛选出的消耗>2000的记录 |

## 📝 数据字段说明

每条记录包含以下字段：

| 字段名 | 说明 |
|--------|------|
| materialId | 物料ID |
| title | 标题 |
| taskId | 任务ID |
| taskName | 任务名称 |
| douyinNickname | 抖音昵称 |
| douyinId | 抖音ID |
| videoUrl | 视频链接 |
| accountName | 账号名称 |
| cost | 消耗金额 |
| productName | 产品名称 |
| date | 日期 |

## ⚠️ 注意事项

1. **浏览器锁定**：在提取过程中，浏览器会被锁定，不要手动操作
2. **网络稳定性**：确保网络连接稳定，避免页面加载失败
3. **数据验证**：提取完成后，检查总记录数是否合理
4. **错误处理**：脚本内置了错误重试机制，最多允许连续3次错误
5. **等待时间**：每页切换后等待2秒，确保数据加载完成

## 🔧 配置调整

如需修改配置，编辑 `auto_extract_browser_console.js` 顶部的 CONFIG 对象：

```javascript
const CONFIG = {
    totalPages: 52,      // 总页数（根据实际情况调整）
    waitTime: 2000,      // 每页等待时间（毫秒）
    minCost: 2000        // 筛选最小消耗值
};
```

## 🐛 故障排查

### 问题1：未找到表格主体元素

**原因**：页面未加载完成或选择器不匹配  
**解决**：
- 检查页面是否完全加载
- 确认表格class名称是否正确（`.arco-table-body`）

### 问题2：提取数据为0条

**原因**：表格行选择器不匹配  
**解决**：
- 在控制台检查：`document.querySelectorAll('tr.arco-table-tr').length`
- 确认数据是否已加载

### 问题3：下一页按钮不可点击

**原因**：已到达最后一页或按钮状态异常  
**解决**：
- 脚本会自动检测并停止
- 手动检查分页状态

### 问题4：数据保存失败

**原因**：文件路径不存在或权限问题  
**解决**：
- 确认 `/workspace` 目录存在
- 检查文件写入权限

## 📈 数据统计示例

运行 `process_extracted_data.py` 后，会看到类似统计：

```
数据统计
============================================================
总记录数: 1024
消耗统计:
  - 总消耗: 1,234,567.89
  - 平均消耗: 1,206.22
  - 最大消耗: 50,000.00
  - 最小消耗: 10.00
账号数: 45
产品数: 12
============================================================
```

## 🎯 下一步

1. 检查生成的CSV文件：`high_cost_records.csv`
2. 根据需要进一步分析高消耗记录
3. 导入Excel或其他工具进行可视化分析

## 📞 技术支持

如遇问题，请检查：
1. 浏览器控制台是否有错误信息
2. 网络连接是否正常
3. 数据结构是否发生变化（网页改版）

---

**最后更新**: 2026-08-07  
**版本**: 1.0